{
  "name": "reflex-manual-test1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Math calculator module (TypeScript) used for manually testing the reflex Diagnose workflow.",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test test/calculator.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
