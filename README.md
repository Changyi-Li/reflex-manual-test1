# reflex-manual-test1

## Math calculator

A small TypeScript module: `src/calculator.ts` with `add`, `subtract`, `multiply`, `divide` and a `calculate(operator, a, b)` dispatcher.

Requires Node.js >= 22.6.

```bash
npm install
npm start    # run the CLI demo
npm test     # run the test suite
npm run typecheck
```